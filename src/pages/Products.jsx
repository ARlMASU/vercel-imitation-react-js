import { useState } from "react";
import Nav from "../components/Nav/Nav";
import Bloc from "../components/Bloc/Bloc";

export default function Products() {
  return (
    <Bloc
      title="A deployment for every idea."
      text="Traditional deployment tools weren’t built for frontend development. Get the DX Platform that has teams shipping faster with higher quality."
    />
  );
}
